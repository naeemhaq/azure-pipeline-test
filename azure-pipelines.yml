# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: updateGroup
  # define b as a counter with the prefix as variable a, and seed as 10.
- name: minor1
  value:  $[counter(variables['major1'], 10)]
- name: sic-version
  value: $(minor1)

steps:
- script: |
    echo $(minor1)
    echo $(major1)
    echo $(sic-version)
    echo '---------------'
  displayName: 'Minor version'

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      $armOutput = @'
      {
          "sqlServerName": {
      	    "value" : "[some sql server name].database.windows.net",
              "type": "string"
          },
          "databaseName": {
      		"value" : "[some sql db name]",
              "type": "string"
          }
      }
      '@
      $armOutputObj = $armOutput | convertfrom-json
      $armOutputObj.PSObject.Properties | ForEach-Object {
          $keyname = $_.Name
          $value = $_.Value.value
          ## Creates a standard pipeline variable
          Write-Output = "##vso[task.setvariable variable=$keyName;]$value"
      		
          ## Creates an output variable
          Write-Output = "##vso[task.setvariable variable=$keyName;isOutput=true]$value"
          Write-Host "##vso[task.setvariable variable=test;]testvalue"
      }

- script: |
    echo "sqlServerName is $(sqlServerName)"
    echo "databaseName from variable $(databaseName)" 
    echo "$env:SYSTEM_ACCESSTOKEN"
    echo "$SYSTEM_ACCESSTOKEN"
    echo "$(System.AccessToken)"

- task: PowerShell@2
  inputs:
    targetType: 'inline'
    script: |
      #$url = "$($env:SYSTEM_TEAMFOUNDATIONCOLLECTIONURI)$env:SYSTEM_TEAMPROJECTID/info/_apis/distributedtask/variablegroups/groupName=naeem-gluu2?api-version=6.1-preview.2"
              Write-Host "URL: $url"
              $pipeline = Invoke-RestMethod -Uri $url -Headers @{
                  Authorization = "Bearer $env:SYSTEM_ACCESSTOKEN"
              }
              Write-Host "Pipeline = $($pipeline | ConvertTo-Json -Depth 100)"
      # Update an existing variable named cb_host to its new value 1035
        $pipeline.variables.cb_host.value = "1035"

        ####****************** update the modified object **************************
        $json = @($pipeline) | ConvertTo-Json -Depth 99
        $updatedef = Invoke-RestMethod -Uri $url -Method Put -Body $json -ContentType "application/json" -Headers @{Authorization = "Bearer $env:SYSTEM_ACCESSTOKEN"}
        write-host "==========================================================" 
        Write-host "The value of Varialbe 'cb_host' is updated to" $updatedef.variables.v1030.value
        write-host "=========================================================="


#https://cloudarchitected.com/2019/10/updating-variable-groups-from-an-azure-devops-pipeline/
# https://developercommunity.visualstudio.com/content/problem/639518/update-single-variable-in-variable-group-which-has.html
- bash: |
     
     $baseUri = "https://dev.azure.com/info0606/info/_apis/distributedtask/variablegroups/4?api-version=5.0-preview.1"
     curl -fL -XPUT -H "Authorization:Bearer $(System.AccessToken)" -H "Content-Type:application/json" \
     -d '{
       "id": "4",
       "type": "Vsts",
       "name": "updateGroup",
       "variables": {
         "test": {
           "isSecret": false,
           "value": "$(sqlServerName)"
         },
         "databaseName": {
           "isSecret": false,
           "value": "$(databaseName)"
         }
       }
     }' \
     $(System.TeamFoundationCollectionUri)/$(System.TeamProject)/_apis/distributedtask/variablegroups/4?api-version=5.0-preview.1
  displayName: 'Set variable in variable group to current foo'

